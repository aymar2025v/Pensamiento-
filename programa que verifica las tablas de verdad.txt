#include <iostream>
using namespace std;
int main()
{
    
    bool a, b;
    cin >> a >> b;
    cout << "and: " << (a and b) << endl;
    cout << "or: " << (a or b) << endl;

    return 0;
}